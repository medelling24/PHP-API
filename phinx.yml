paths:
    migrations: db/migrations

environments:
    default_migration_table: phinxlog
    default_database: development
    production:
        adapter: mysql
        host: localhost
        name: my_app
        user: my_app
        pass: 'secret'
        port: 3306
        charset: utf8

    development:
        adapter: mysql
        host: localhost
        name: my_app
        user: my_app
        pass: 'secret'
        port: 3306
        charset: utf8